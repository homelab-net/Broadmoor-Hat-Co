# Broadmoor-Hat-Co

## Definitive implementation guide

Use `BROADMOOR_HAT_DESIGNER_AND_FIND_YOUR_HAT_IMPLEMENTATION_SPEC.txt` as the authoritative build spec.

## Coder starting base

Use `theme-scaffold/` as the cleaned-up, spec-aligned foundation for Shopify theme implementation.

## Legacy prototype references

Historical prototype and v1 preview artifacts are retained for reference in `prototype/` and `deliverable/v1/`.
